--// This file was created by XHider v1.2 [https://discord.gg/hATuHQaQRb]

local bearlib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Myvkhuy/One-Ui/refs/heads/Bearlib/bearlib.lua"))()
local Window = bearlib:MakeWindow({
    Title = "Bear hub | Farm",
    SubTitle = "by Quang Huy",
    SaveFolder = true,
    Image = "82107905019656"
})
local CombatTab = Window:MakeTab({
    Title = "Combat",
    Icon = "rbxassetid://10734950020"
})
local targetDisplay = CombatTab:AddParagraph({
    Title = "🎯 Current Target",
    Desc = "Searching..."
})
            local target = getgenv().GunAuraSettings.CurrentTarget.Parent
                local targetType = "NPC"
                local hum = target:FindFirstChild("Humanoid")
                local healthText = hum and string.format(" - %.1f/%.1f HP (%.0f%%)", hum.Health, hum.MaxHealth, (hum.Health / hum.MaxHealth) * 100) or ""
                local myPos = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:GetPivot().Position
                local targetPos = getgenv().GunAuraSettings.CurrentTarget.Position
                local distText = ""
                    local dist = math.floor((targetPos - myPos).Magnitude)
local VirtualInputManager = game:GetService("VirtualInputManager")
local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Modules = ReplicatedStorage:WaitForChild("Modules")
local Net = Modules:WaitForChild("Net")
local RegisterAttack = Net:WaitForChild("RE/RegisterAttack")
local RegisterHit = Net:WaitForChild("RE/RegisterHit")
local function IsAlive(char)
    return char and char:FindFirstChild("Humanoid") and char.Humanoid.Health > 0
end
local AttackDelays = {
    ["Normal Attack"] = 0.25,
    ["Fast Attack"] = 0.15,
    ["Super Attack"] = 0.05,
    ["Bear Attack"] = 0.1,
    ["Super Bear Attack"] = 0.01,
}
local FastAttack1 = { Distance = 100 }
    local OthersEnemies = {}
    local EnemiesFolder = workspace:FindFirstChild("Enemies")
    local CharactersFolder = workspace:FindFirstChild("Characters")
    local BasePart = nil
    local function ProcessFolder(folder)
        if not folder then return BasePart end
        for _, enemy in ipairs(folder:GetChildren()) do
            local head = enemy:FindFirstChild("Head")
            if head and IsAlive(enemy) and enemy ~= Player.Character then
                local dist = (Player.Character.HumanoidRootPart.Position - head.Position).Magnitude
                if dist < FastAttack1.Distance then
                    table.insert(OthersEnemies, { enemy, head })
                    BasePart = head
                end
            end
        end
        return BasePart
    end
local FastAttack2 = { Distance = 100 }
    local OthersEnemies = {}
    local EnemiesFolder = workspace:FindFirstChild("Enemies")
    local CharactersFolder = workspace:FindFirstChild("Characters")
    local BasePart = nil
    local function ProcessFolder(folder)
        if not folder then return nil end
        for _, enemy in ipairs(folder:GetChildren()) do
            local head = enemy:FindFirstChild("Head")
            if head and IsAlive(enemy) and enemy ~= Player.Character then
                local dist = (Player.Character.HumanoidRootPart.Position - head.Position).Magnitude
                if dist < FastAttack2.Distance then
                    table.insert(OthersEnemies, { enemy, head })
                    BasePart = head
                end
            end
        end
        return BasePart
    end
    local Part1 = ProcessFolder(EnemiesFolder)
    local Part2 = ProcessFolder(CharactersFolder)
    local delayTime = AttackDelays[mode] or 0.15
            local char = Player.Character
                local tool = char:FindFirstChildOfClass("Tool")
            local char = Player.Character
                local tool = char:FindFirstChildOfClass("Tool")
            local char = Player.Character
local Camera = workspace.CurrentCamera
local Enemies = workspace:FindFirstChild("Enemies") or Instance.new("Folder", workspace)
local Characters = workspace:FindFirstChild("Characters") or Instance.new("Folder", workspace)
local ShootGunEvent = Net:FindFirstChild("RE/ShootGunEvent")
local mt = getrawmetatable(game)
local old_namecall = mt.__namecall
    local method = getnamecallmethod()
    local args = {...}
local function setSimulationRadius()
    pcall(function()
        sethiddenproperty(Player, "SimulationRadius", 10000)
    end)
end
local function findClosestTargetForGun()
    local myPos = Player.Character and Player.Character:GetPivot().Position
    if not myPos then return nil end
    setSimulationRadius()
    local closestDist = getgenv().GunAuraSettings.GunRange + 1
    local closestTarget = nil
    local closestPart = nil
    
    local targetType = getgenv().GunAuraSettings.TargetType
    if targetType == "NPC" or targetType == "All" then
        for _, enemy in ipairs(Enemies:GetChildren()) do
            if IsAlive(enemy) and enemy ~= Player.Character then
                local hrp = enemy:FindFirstChild("HumanoidRootPart") or enemy:FindFirstChild("Head")
                if hrp then
                    local dist = (hrp.Position - myPos).Magnitude
                    if dist < closestDist then
                        closestDist = dist
                        closestTarget = enemy
                        closestPart = hrp
                    end
                end
            end
        end
    end
    if targetType == "Player" or targetType == "All" then
        for _, char in ipairs(Characters:GetChildren()) do
            if IsAlive(char) and char ~= Player.Character then
                local hrp = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Head")
                if hrp then
                    local dist = (hrp.Position - myPos).Magnitude
                    if dist < closestDist then
                        closestDist = dist
                        closestTarget = char
                        closestPart = hrp
                    end
                end
            end
        end
    end
    return closestTarget, closestPart
end
local function applyGunDamage()
    if not getgenv().GunAuraSettings.GunEnabled then return end
    if not Player.Character or not IsAlive(Player.Character) then 
        getgenv().GunAuraSettings.CurrentTarget = nil
        return 
    end
    local target, targetPart = findClosestTargetForGun()
    if not target or not targetPart then 
        getgenv().GunAuraSettings.CurrentTarget = nil
        return 
    end
    getgenv().GunAuraSettings.CurrentTarget = targetPart
    if ShootGunEvent then
        pcall(function()
            for i = 1, 3 do
                ShootGunEvent:FireServer(targetPart.Position, {targetPart})
            end
        end)
    end
end
local function performClick()
    if not getgenv().AutoClickShootSettings.Enabled then return end
    if not Player.Character or not Player.Character:FindFirstChildOfClass("Tool") then return end
    
    local currentTime = tick()
    if currentTime - getgenv().AutoClickShootSettings.LastClickTime < getgenv().AutoClickShootSettings.ClickDelay then
        return
    end
    
    local clickPosition = Vector2.new(Camera.ViewportSize.X / 2, 20)
    VirtualInputManager:SendMouseButtonEvent(clickPosition.X, clickPosition.Y, 0, true, game, 0)
    task.wait(0.02)
    VirtualInputManager:SendMouseButtonEvent(clickPosition.X, clickPosition.Y, 0, false, game, 0)
    
    getgenv().AutoClickShootSettings.LastClickTime = currentTime
end

local _v1 = game:HttpGet("https://raw.githubusercontent.com/Myvkhuy/One-Ui/refs/heads/Bearlib/bearlib.lua")
local _v2 = loadstring(_v1)()
local _v3 = _v2:MakeWindow({
    Image = "82107905019656",
    SaveFolder = true,
    SubTitle = "by Quang Huy",
    Title = "Bear hub | Farm"
})
local _v4 = _v3:MakeTab({Icon = "rbxassetid://10734950020", Title = "Combat"})
local _v5 = _v4:AddSection({"\xe2\x9a\x94\xef\xb8\x8f Fast Attack"})
local _v6 = _v4:AddDropdown({
    Callback = function() end,
    Default = "Fast Attack",
    Multi = false,
    Name = "Select Attack Mode",
    Options = {
        "Normal Attack",
        "Fast Attack",
        "Super Attack",
        "Bear Attack",
        "Super Bear Attack"
    }
})
local _v7 = _v4:AddToggle({
    Callback = function()
    local _ = _G.FastAttackLoop1
    local _ = _G.FastAttackLoop2
    local _ = _G.AutoClickLoop
    task.spawn(function(...) end)
    local FastAttackLoop1 = nil
    task.spawn(function(...) end)
    local FastAttackLoop2 = nil
    task.spawn(function(...) end)
    local AutoClickLoop = nil
end,
    Default = false,
    Name = "Enable Fast Attack"
})
local _v8 = _v4:AddToggle({
    Callback = function() end,
    Default = false,
    Name = "Enable Auto Click"
})
local _v9 = _v4:AddSection({"\xf0\x9f\x94\xab Gun Aura"})
local _v10 = _v4:AddDropdown({
    Callback = function() end,
    Default = "All",
    Multi = false,
    Name = "Target Type",
    Options = {"Player", "NPC", "All"}
})
local _v11 = _v4:AddSlider({
    Callback = function() end,
    Default = 500,
    Increment = 10,
    Max = 30000,
    Min = 50,
    Name = "Gun Range"
})
local _v12 = _v4:AddToggle({
    Callback = function() end,
    Default = false,
    Name = "Auto Gun Aura"
})
local _v13 = _v4:AddToggle({
    Callback = function() end,
    Default = false,
    Name = "Auto Click Gun (2s delay)"
})
local _v14 = _v4:AddParagraph({Desc = "Searching...", Title = "\xf0\x9f\x8e\xaf Current Target"})
local _v = task.spawn(function(...)
    task.wait(0.5)
    local _v18 = _v14:SetDesc("Searching...")
    task.wait(0.5)
    local _v19 = _v14:SetDesc("Searching...")
    task.wait(0.5)
    local _v20 = _v14:SetDesc("Searching...")
end)
local VirtualInputManager = game:GetService("VirtualInputManager")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local modules = ReplicatedStorage:WaitForChild("Modules")
local net = modules:WaitForChild("Net")
local re/registerattack = net:WaitForChild("RE/RegisterAttack")
local re/registerhit = net:WaitForChild("RE/RegisterHit")
local _ = _G.Settings
local Settings = {}
local StopAllAttacks = function() end
local StartAttacksWithMode = function()
    task.spawn(function(...) end)
    local FastAttackLoop1 = nil
    task.spawn(function(...) end)
    local FastAttackLoop2 = nil
    task.spawn(function(...) end)
    local AutoClickLoop = nil
end
local _v15 = workspace:FindFirstChild("Enemies")
local _v16 = workspace:FindFirstChild("Characters")
_v15.Name = "Enemies"
_v16.Name = "Characters"
local _v17 = net:FindFirstChild("RE/ShootGunEvent")
local genv = getgenv()
genv.GunAuraSettings = {
    Enabled = false,
    FireRate = 0.01,
    GunEnabled = false,
    GunRange = 500,
    TargetType = "All"
}
genv.AutoClickShootSettings = {ClickDelay = 2, Enabled = false, LastClickTime = 0}
local _v = task.spawn(function(...)
    local _v250018 = Players.LocalPlayer.Character:FindFirstChild("Humanoid")
    task.wait(0.01)
    local _v250019 = Players.LocalPlayer.Character:FindFirstChild("Humanoid")
    task.wait(0.01)
    local _v250020 = Players.LocalPlayer.Character:FindFirstChild("Humanoid")
    task.wait(0.01)
end)
local _v = task.spawn(function(...)
    local _v500019 = Players.LocalPlayer.Character:FindFirstChildOfClass("Tool")
    local _v500020 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v500021 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
    task.wait(0.1)
    local _v500022 = Players.LocalPlayer.Character:FindFirstChildOfClass("Tool")
    task.wait(0.1)
    local _v500023 = Players.LocalPlayer.Character:FindFirstChildOfClass("Tool")
    task.wait(0.1)
    local _v526902 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v526903 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
    local _v553866 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v553867 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
    local _v580262 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v580263 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
    local _v607077 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v607078 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
    local _v632589 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v632590 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
    local _v659227 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v659228 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
    local _v684118 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v684119 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
    local _v710089 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v710090 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
    local _v735120 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, true, game, 0)
    task.wait(0.02)
    local _v735121 = VirtualInputManager:SendMouseButtonEvent(Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).X, Vector2.new((workspace.CurrentCamera.ViewportSize.X / 2), 20).Y, 0, false, game, 0)
end)
local _v = task.spawn(function(...)
    task.wait(5)
    task.wait(5)
    task.wait(5)
end)
print("\xe2\x9c\x85 Combat Tab loaded!")